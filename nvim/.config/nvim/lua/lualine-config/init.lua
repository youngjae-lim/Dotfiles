require('lualine').setup({
  options = {
    -- @TODO: change theme variable dynamically upon selecting a new colortheme from a telescope color picker
    theme = 'rose-pine',
    section_separators = '',
    component_separators = ''
  }
})
